import 'package:flutter/material.dart';

class SMapScreen extends StatelessWidget {
  const SMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("map"),
    );
  }
}